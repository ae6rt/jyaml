Read os.Stdin and convert to or from JSON / YAML.

```
Usage of ./jyaml:
  -o string
        output format (j=json, y=yaml)
```

If the output format is "y", the input is presumed to be JSON; if "j", YAML.
